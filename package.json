{
  "name": "withvibe",
  "version": "0.4.5",
  "description": "withvibe CLI — install/manage the server stack and run shared environments locally.",
  "license": "Apache-2.0",
  "homepage": "https://withvibe.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/withvibe/withvibe-cli.git"
  },
  "bugs": {
    "url": "https://github.com/withvibe/withvibe-cli/issues"
  },
  "type": "module",
  "bin": {
    "withvibe": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run bundle-assets",
    "bundle-assets": "node -e \"const fs=require('fs');fs.mkdirSync('dist/assets',{recursive:true});fs.copyFileSync('assets/docker-compose.yml','dist/assets/docker-compose.yml')\"",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "open": "^10.1.0",
    "ora": "^8.1.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/prompts": "^2.4.9",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
